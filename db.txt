ALTER TABLE `students` ADD `objective` TEXT NULL DEFAULT NULL AFTER `permanent_district_id`, ADD `present_salary` DECIMAL(6) NULL DEFAULT NULL AFTER `objective`, ADD `expected_salary` DECIMAL(6) NULL DEFAULT NULL AFTER `present_salary`, ADD `looking_for` ENUM("Entry Level","Mid Level","Top Level") NULL DEFAULT NULL COMMENT 'Job Level' AFTER `expected_salary`, ADD `available_for` ENUM("Full Time","Part Time","Contract","Internship","Freelance") NULL DEFAULT NULL COMMENT 'Job Nature' AFTER `looking_for`, ADD `career_summary` TEXT NULL DEFAULT NULL AFTER `available_for`, ADD `special_qualification` TEXT NULL DEFAULT NULL AFTER `career_summary`, ADD `keywords` VARCHAR(255) NULL DEFAULT NULL AFTER `special_qualification`; 

ALTER TABLE `student_education` CHANGE `edu_group_id` `edu_group_id` BIGINT UNSIGNED NULL DEFAULT NULL; 
